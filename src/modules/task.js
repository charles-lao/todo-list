/* eslint-disable arrow-body-style */

const Task = (idNo, title, description, dueDate, priority, project) => {
  return { idNo, title, description, dueDate, priority, project };
};

export default Task;
